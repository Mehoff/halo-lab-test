import Category from "../../entities/category";

const CATEGORY_HORROR: Category = { name: "Horror" };
const CATEGORY_DOCUMENTARY: Category = { name: "Documentary" };
const CATEGORY_COMEDY: Category = { name: "Comedy" };

const Categories: Category[] = [
  CATEGORY_HORROR,
  CATEGORY_DOCUMENTARY,
  CATEGORY_COMEDY,
];

export default Categories;
